# Build output — regenerated by `pnpm build` / GHA on every deploy.
# Unlike apps/landing/deploy/ (which IS tracked because it doubles as
# a GitHub Pages source), trail-docs ships only via Fly + GHA, so the
# build output is pure ephemeral artifact.
deploy/
node_modules/
*.tgz
