# Upmetrics browser telemetry DSN — PUBLIC (Sentry-style, ships in the JS bundle).
# Single source for both local `pnpm ship:admin` and CI `pnpm deploy` Vite builds;
# inlined by Vite at build time as import.meta.env.VITE_UPMETRICS_DSN.
# Committed on purpose (see the !apps/admin/.env.production negation in .gitignore).
VITE_UPMETRICS_DSN=https://473187ffbb6aa1bee4eb3d60e469f525@upmetrics.org/trail
